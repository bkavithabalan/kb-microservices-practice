spring:
  application:
    name: auth-service
server.port: 8200
logging:
  level:
    org.springframework: info
eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8762/eureka
  instance:
    prefer-ip-address: true	