server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///config-repo
eureka:
  client:
    serviceUrl.defaultZone: ${EUREKA_URI:http://localhost:8762/eureka}