server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/MyLearnings/Git/local-git-repo
eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8762/eureka