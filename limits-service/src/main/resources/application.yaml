spring:
  application:
    name: limits-service
  config:
    import: optional:configserver:http://localhost:8888
limits-service:
  minimum: 2
  maximum: 2007
  
management:
  endpoints:
    web:
      exposure:
        include: "*"
#health,info,metrics,beans,refresh
eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8762/eureka
